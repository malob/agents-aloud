# Agent Instructions

Read `CLAUDE.md` first. It is the authoritative project context for this repo,
including architecture, load-bearing implementation decisions, tooling, and
style guidance.

Keep this file as a pointer rather than duplicating those instructions, so the
agent guidance does not drift between tools.
